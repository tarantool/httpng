name: Test buildability, then regressions

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        #os: ['ubuntu-latest', 'macos-latest']
        os: ['macos-latest']
        #tarantool-ref: ['1.10', '2.7']
        tarantool-ref: ['1.10', 'void234/hunt-for-httpng-macos-deadlock']
        #enable_asan: [true, false]
        enable_asan: [false]
        exclude:
          - os: 'macos-latest'
            tarantool-ref: '1.10'
    runs-on: ${{matrix.os}}

    steps:
    - name: Where we are?
      run: uname -a; cat /etc/os-release || cat /etc/issue || python -m platform || true

    - uses: actions/checkout@v2

    - name: Checkout submodules
      run: git submodule update --init --recursive

    - name: Build and install Tarantool (with ASAN)
      uses: ./.github/actions/build-tarantool
      if: ${{matrix.enable_asan}}
      with:
        cc: 'clang'
        cxx: 'clang++'
        cmake_defines: '-DENABLE_ASAN=ON'
        ref: ${{matrix.tarantool-ref}}

    - name: Build and install Tarantool (without ASAN)
      uses: ./.github/actions/build-tarantool
      if: ${{!matrix.enable_asan}}
      with:
        ref: ${{matrix.tarantool-ref}}

    - name: Build httpng (with ASAN)
      uses: ./.github/actions/build-httpng
      if: ${{matrix.enable_asan}}
      with:
        cc: 'clang'
        cxx: 'clang++'
        cmake_defines: '-DENABLE_ASAN=ON'

    - name: Build httpng (without ASAN)
      uses: ./.github/actions/build-httpng
      if: ${{!matrix.enable_asan}}

    - name: Add entries to /etc/hosts
      run: sudo tests/mangle_etc_hosts.sh

    - name: Install luatest
      working-directory: ${{github.workspace}}/build
      # Ignore all errors (some are caused by ASAN in Tarantool)
      run: ASAN_OPTIONS=detect_leaks=0 ${{github.workspace}}/build/tarantool/build/extra/dist/tarantoolctl rocks install luatest

    - name: Test HTTPNG (luatest)
      working-directory: ${{github.workspace}}
      # FIXME: luatest doesn't understand neither absolute paths nor ".."!
      # FIXME: Should enable Leak Sanitizer some day...
      #run: ulimit -n 65536 && ASAN_OPTIONS=detect_leaks=0 LUA_CPATH="${{github.workspace}}/build/?.dylib;${{github.workspace}}/build/?.so" build/.rocks/bin/luatest -v -r 1 tests/all.lua --shuffle all
      run: |
        bash ./5.sh &
        ulimit -n 65536 && echo "set startup-with-shell off" >> ~/.gdbinit && LUA_PATH="${{github.workspace}}/build/.rocks/share/tarantool/?/init.lua;${{github.workspace}}/build/.rocks/share/tarantool/?.lua;" LUA_CPATH="${{github.workspace}}/build/?.dylib;${{github.workspace}}/build/?.so" ASAN_OPTIONS=detect_leaks=0 tarantool 3.lua
